# Solnova Digital Twin — copy values from your Vercel project settings
# These are embedded in twin.js at build time — update twin.js directly for vanilla JS apps
SUPABASE_URL=https://bxewkghaljeucxekwltd.supabase.co
# SUPABASE_ANON_KEY is public (anon key) — already embedded in twin.js
TWIN_URL=https://solnova.app
